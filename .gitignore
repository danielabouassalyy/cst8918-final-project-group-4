# ── Terraform (keep lockfile) ──────────────────────────────────────
.terraform/               # local plugin/module cache
*.tfstate
*.tfstate.*
crash.log
*.tfvars
*.tfvars.json
*.auto.tfvars
# plan files
*.tfplan
tfplan

# explicitly keep the lock file (tracked on purpose)
!.terraform.lock.hcl

# ── Local/private files you created ────────────────────────────────
fc-*.json
oldstate-backup.json
ghapp.json

# ── Editor / OS junk ───────────────────────────────────────────────
.vscode/
.DS_Store
Thumbs.db
